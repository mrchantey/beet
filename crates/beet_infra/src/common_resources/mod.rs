//! Pre-generated provider bindings for commonly used resources.
//!
//! Each sub-module is gated behind a cargo feature so that only the providers
//! your project needs are compiled.

// #[cfg(feature = "common_resources_aws_lambda")]
// pub mod aws_lambda;

// #[cfg(feature = "common_resources_aws_lightsail")]
// pub mod aws_lightsail;

// #[cfg(feature = "common_resources_cloudflare_dns")]
// pub mod cloudflare_dns;
