[package]
name = "sweet"
version = "0.4.2"
edition.workspace = true
license.workspace = true
authors.workspace = true
readme = "README.md"
description = "A delightful cross-platform test runner"
documentation.workspace = true
repository.workspace = true
categories.workspace = true
keywords.workspace = true

[features]
# see readme for feature docs
default = ["runner", "serde", "nightly"]
runner = ["dep:beet_core", "serde"]
nightly = []
tokens = ["dep:proc-macro2", "dep:syn", "dep:quote", "dep:prettyplease"]
serde = ["dep:ron"]

[dependencies]
#💡 internal
beet_core = { workspace = true, optional = true, features=["testing"] }
sweet_macros.workspace = true
#💡 misc
extend.workspace = true
send_wrapper.workspace = true

#💡 logging
nu-ansi-term.workspace = true


#💡 bevy
bevy.workspace = true

#💡 tokens
proc-macro2 = { workspace = true, optional = true }
syn = { workspace = true, optional = true }
quote = { workspace = true, optional = true }
prettyplease = { workspace = true, optional = true }

#💡 serde
ron = { workspace = true, optional = true }

#💡 wasm
[target.'cfg(target_arch = "wasm32")'.dependencies]
console_error_panic_hook.workspace = true

[dev-dependencies]
sweet = {path = ".", features = ["runner", "tokens"] }
