# beet_examples

Shared scaffolding for beet's larger examples.

This is an internal crate, not meant to be depended on directly. It bundles the components, plugins and scenes reused across the `spatial` and `ml` examples so each example file can stay focused on the feature it demonstrates.
