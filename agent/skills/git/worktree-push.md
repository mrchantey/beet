# Worktree Push

Time to merge this worktree upstream. 
- The repo lives at `~/me/beet`.
- Do not delete this worktree.
- Carefully resolve all conflicts
- Run a sweep of the tests in the repo to ensure all working as expected, fix any issues. you may commit the merge and resolved conflicts but dont commit the fixed issues, leave those as unstaged changes so i can review
```sh
just test-core | tail
```
