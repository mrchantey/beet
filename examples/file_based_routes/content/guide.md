# Guide

This page is **markdown** content, served at request time by a `BlobScene`
reading from the collection's `BlobStore`.

- no content codegen
- parsed to entities + frontmatter on each request
