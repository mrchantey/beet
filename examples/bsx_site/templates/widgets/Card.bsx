<!-- A site-local template: resolves as <widgets::Card> from any page.
     The `title` prop binds into the heading via `@prop:`, caller content
     routes into the default slot. -->
<section class="card-filled">
	<h2>{@prop:title}</h2>
	<Slot/>
</section>
